// Landing-page config. Drop the hosted demo video URL here once it's on
// Cloudflare Stream/R2 (a public MP4 URL or an <iframe> embed URL). While empty,
// the "View demo" button still renders and opens a "coming soon" placeholder so
// the layout stays final.
export const DEMO_VIDEO_URL = '';

/** true for a direct file (<video>), false for an embed page (<iframe>). */
export const DEMO_VIDEO_IS_FILE = true;
